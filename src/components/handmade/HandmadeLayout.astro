---
import BaseLayout from "@components/base/BaseLayout.astro";
import HandmadeNav from "@components/handmade/HandmadeNav.astro";
import PageHeader from "@components/common/PageHeader.astro";

import type { TocItem } from "@components/handmade/HandmadeNav.astro";

interface Props {
  title: string;
  description?: string;
  toc?: TocItem[];
}

const { title, description, toc } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <PageHeader title="Handmade" colorScheme="cyan" />

  <section class="container p-4">
    <div class="flex flex-col gap-8 lg:grid lg:grid-cols-4">
      {/* Navigation: First in HTML for mobile top placement */}
      <div class="order-1 lg:order-2 lg:col-span-1">
        <HandmadeNav toc={toc}>
          <slot name="nav-extra" />
        </HandmadeNav>
      </div>
      {/* Content: Second in HTML for mobile bottom placement */}
      <div class="order-2 space-y-6 lg:order-1 lg:col-span-3">
        <slot />
      </div>
    </div>
  </section>
</BaseLayout>
