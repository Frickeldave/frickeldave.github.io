---
visible: true
title: 03 Vorbereitung
description: Entwicklungsumgebung einrichten und ein neues Starlight-Projekt anlegen.
draft: false
image: "@assets/docs/astro-workshop/asws-03-001.png"
imageAlt: "Vorbereitung"
---

> ⚠️ **PREVIEW**–Dieser Inhalt befindet sich noch in Arbeit und kann noch Änderungen unterliegen.

## Voraussetzungen

- Grundkenntnisse in JavaScript oder TypeScript (wirklich nur basic)
- Grundlegende Git-Kenntnisse (clone, commit, push)
- Vertrautheit mit der Kommandozeile (bash, zsh oder PowerShell)

## Software installieren

Grundsätzlich empfehle ich Dir die Installation der gesamten Umgebung unter Linux Debian. Getestet sind die folgend gezeigten 
Scripte mit Debian 13.4. 
Arbeitest du auf einem Windows System, empfehle ich die Installation einer WSL mit debian. Unter MAC sollten die folgend gezeigten
Linux Scripte adaptierbar sein, sind aber nicht getestet. 

### Node.js

Astro benötigt Node.js in aktueller LTS Version.

```bash
# Version prüfen
node --version
npm --version
```

**Installation:**

- **Windows:** [nodejs.org/de/download](https://nodejs.org/de/download/)—Installer herunterladen
- **macOS (Homebrew):** `brew install node`
- **Linux (apt):** `sudo apt-get update && sudo apt-get install -y nodejs npm`

> **Tipp:** Wer mehrere Node.js-Versionen parallel verwalten möchte, nutzt
> [nvm](https://github.com/nvm-sh/nvm) (Linux/macOS) oder
> [nvm-windows](https://github.com/coreybutler/nvm-windows).

Beispiel unter Linux

```bash
cd ~
sudo apt install git curl -y
curl -fsSL https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion
nvm install --lts
nvm use --lts
# Make nvm available in non-interactive login shells (bash -lc)
grep -qF 'NVM_DIR' ~/.profile || printf '\nexport NVM_DIR="$HOME/.nvm"\n[ -s "$NVM_DIR/nvm.sh" ] && \\. "$NVM_DIR/nvm.sh"\n' >> ~/.profile
```

### Git

```bash
# Version prüfen
git --version
```

**Installation:**

- **Windows:** [git-scm.com/download/win](https://git-scm.com/download/win)
- **macOS (Homebrew):** `brew install git`
- **Linux (apt):** `sudo apt-get install -y git`

### VS Code (empfohlen)

VS Code ist der empfohlene Editor für diesen Workshop. Die folgenden Extensions sind hilfreich:

| Extension                    | Publisher  | Zweck                              |
| ---------------------------- | ---------- | ---------------------------------- |
| Astro                        | astro-build | Syntax-Highlighting, IntelliSense |
| ESLint                       | Microsoft  | ESLint-Fehler direkt im Editor     |
| Prettier                     | Prettier   | Formatierung on Save               |
| Vale VSCode                  | ChrisChinchilla | Vale-Warnungen im Editor      |
| Starlight                    | astro-build | Starlight-spezifische Syntax und Snippets |

## Starlight-Projekt anlegen

Als Projektbasis dient **Starlight**—das offizielle Astro-Dokumentations-Framework. Es wird
direkt über den Astro-Installer erstellt, kein Fork nötig.

### Schritt 1: Projekt erstellen

```bash
npm create astro@latest -- --template starlight astrows
cd astrows
```

Der Installer stellt ein paar Fragen—empfohlene Antworten:

| Frage | Empfehlung |
|---|---|
|Ok to proceed? (y)|y|
| Install dependencies? | Yes |
| Initialize a new git repository? | Yes |
| TypeScript? | Strict |

### Schritt 2: Dev-Server starten

```bash
npm run dev
```

Die Website ist anschließend unter [http://localhost:4321](http://localhost:4321) erreichbar.

**GitHub Pages: Welcher Repository-Name?**

GitHub Pages unterscheidet zwei Varianten:

| Typ | Repository-Name | URL |
|---|---|---|
| User/Org Page | Muss exakt `USERNAME.github.io` heißen | `https://USERNAME.github.io` |
| Project Page | Beliebig, z. B. `astrows` | `https://USERNAME.github.io/astrows` |

Im Workshop verwenden wir eine **Project Page** mit dem Namen `astrows`—der
User-Page-Slot (`USERNAME.github.io`) ist bei vielen bereits belegt. Bei einer Project Page
muss in `astro.config.mjs` zusätzlich `base: "/astrows/"` gesetzt werden
(siehe [Astro Setup](10-astro-setup)).

### Schritt 3: Repository auf GitHub anlegen und pushen

```bash
# Auf github.com/new ein neues leeres Repository "astrows" mit default Einstellungen anlegen, dann:
git add -A
git commit -m "chore: initial starlight setup"
# Falls "nothing to commit" und noch kein Commit existiert:
# git commit --allow-empty -m "chore: initial commit"
git branch -M main
git remote add origin https://github.com/DEIN-USERNAME/astrows.git
git push -u origin main
```

## Projektstruktur im Überblick

Nach der Installation ergibt sich folgende Struktur:

```
astrows/
├── public/              # Statische Ressourcen (Favicon, robots.txt)
├── src/
│   ├── assets/          # Bilder (werden von Astro optimiert)
│   ├── content/
│   │   └── docs/        # Markdown/MDX-Dokumentationsseiten ← hier arbeiten wir
│   └── content.config.ts # Docs-Collection Schema
├── astro.config.mjs     # Astro + Starlight Konfiguration
├── package.json         # Abhängigkeiten und npm-Scripts
└── tsconfig.json        # TypeScript-Konfiguration
```

Starlight ist bewusst schlank gehalten—keine `components/`, keine `pages/`, keine `styles/`
überladen mit Custom-Code. Neue Dokumentationsseiten entstehen einfach als `.md`- oder
`.mdx`-Dateien in `src/content/docs/`.

## Hardware-Empfehlungen

| Komponente  | Minimum    | Empfohlen  |
| ----------- | ---------- | ---------- |
| RAM         | 4 GB       | 8 GB       |
| CPU         | 2 Kerne    | 4 Kerne    |
| Festplatte  | 5 GB frei  | 10 GB frei |
| Netzwerk    | Internetzugang | Internetzugang |


## Bestehendes Projekt einrichten

Falls du ein bereits bestehendes Astro-Projekt (z. B. dieses Workshop-Repository) in einer neuen
Umgebung einrichten möchtest, sind nur wenige Schritte nötig.

**Schritt 1: Alle Voraussetzungen installieren**

Installiere alle Voraussetzungen wie oben beschrieben

**Schritt 2: Repository klonen**

```bash
git clone https://github.com/DEIN-USERNAME/astrows.git
cd astrows
```

**Schritt 3: Dependencies installieren**

```bash
npm install
```

Dieser Befehl liest die Datei [`package.json`](package.json) und installiert alle dort
aufgeführten Abhängigkeiten in den Ordner `node_modules/`. Das kann je nach Internetverbindung
einige Minuten dauern.

> **Tipp:** Bei langsamer Verbindung hilft `npm install --verbose`, um den Fortschritt zu
> verfolgen. Falls Probleme auftreten, kann ein `npm cache clean --force` vorab helfen.

Um eventuelle Sicherheitslücken in Abhängigkeiten zu schliessen, lass direkt danach 
(unter Umständen auch mehrmals) ein Update laufen: 

```bash
npm audit fix
```

**Schritt 4: Dev-Server starten**

```bash
npm run dev
```

Der Dev-Server startet und die Website ist unter [http://localhost:4321](http://localhost:4321)
erreichbar.

Sollten Fehler auftreten, prüfe:

- Ist Node.js korrekt installiert? (`node --version`)
- Ist die Node-Version kompatibel? (LTS v20+ empfohlen)
- Gibt es Schreibrechte im Verzeichnis?

Weiter: [Astro Setup](10-astro-setup)
