---
import BaseLayout from "@components/base/BaseLayout.astro";
import { getLinkMappings } from "@/lib/redirectService";
import type { LinkMapping } from "@/types/redirect";

const mappings = await getLinkMappings();
const { links, analytics } = mappings;

// Sort links by click count (descending)
const sortedLinks = Object.entries(links).sort(
  ([, a], [, b]) => (b as LinkMapping).clicks - (a as LinkMapping).clicks
);

const pageTitle = "Link Analytics Dashboard";
const pageDescription =
  "Übersicht über alle verfolgten Links und deren Klick-Statistiken.";
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <main class="container mx-auto px-4 py-8">
    <div class="mx-auto max-w-6xl">
      <!-- Header -->
      <div class="mb-8">
        <h1 class="mb-2 text-4xl font-bold">Link Analytics</h1>
        <p class="text-gray-600 dark:text-gray-300">
          Übersicht über alle verfolgten Links und deren Performance
        </p>
      </div>

      <!-- Summary Stats -->
      <div class="mb-8 grid grid-cols-1 gap-6 md:grid-cols-3">
        <div class="glass rounded-xl p-6">
          <h3 class="mb-2 text-lg font-semibold">Gesamt Klicks</h3>
          <p class="text-3xl font-bold text-blue-600">
            {analytics.totalClicks}
          </p>
        </div>
        <div class="glass rounded-xl p-6">
          <h3 class="mb-2 text-lg font-semibold">Aktive Links</h3>
          <p class="text-3xl font-bold text-green-600">
            {Object.keys(links).length}
          </p>
        </div>
        <div class="glass rounded-xl p-6">
          <h3 class="mb-2 text-lg font-semibold">Letzte Aktualisierung</h3>
          <p class="text-lg font-medium">{analytics.lastUpdated}</p>
        </div>
      </div>

      <!-- Links Table -->
      <div class="glass overflow-hidden rounded-xl">
        <div class="border-b border-gray-200 p-6 dark:border-gray-700">
          <h2 class="text-2xl font-bold">Link Übersicht</h2>
        </div>

        <div class="overflow-x-auto">
          <table class="w-full">
            <thead class="bg-gray-50 dark:bg-gray-800">
              <tr>
                <th
                  class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400"
                >
                  Link ID
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400"
                >
                  Titel
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400"
                >
                  Provider
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400"
                >
                  Kategorie
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400"
                >
                  Klicks
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400"
                >
                  Typ
                </th>
                <th
                  class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider text-gray-500 dark:text-gray-400"
                >
                  Erstellt
                </th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 dark:divide-gray-700">
              {
                sortedLinks.map(([id, linkData]) => {
                  const link = linkData as LinkMapping;
                  return (
                    <tr class="hover:bg-gray-50 dark:hover:bg-gray-800">
                      <td class="whitespace-nowrap px-6 py-4">
                        <code class="rounded bg-gray-100 px-2 py-1 text-sm dark:bg-gray-800">
                          {id}
                        </code>
                      </td>
                      <td class="px-6 py-4">
                        <div class="text-sm font-medium">{link.title}</div>
                        <div class="max-w-xs truncate text-sm text-gray-500 dark:text-gray-400">
                          {link.description}
                        </div>
                      </td>
                      <td class="whitespace-nowrap px-6 py-4">
                        <span class="text-sm">{link.provider}</span>
                      </td>
                      <td class="whitespace-nowrap px-6 py-4">
                        <span class="rounded-full bg-blue-100 px-2 py-1 text-xs font-medium text-blue-800 dark:bg-blue-900 dark:text-blue-200">
                          {link.category}
                        </span>
                      </td>
                      <td class="whitespace-nowrap px-6 py-4">
                        <span class="text-lg font-bold">{link.clicks}</span>
                      </td>
                      <td class="whitespace-nowrap px-6 py-4">
                        {link.affiliate ? (
                          <span class="rounded-full bg-yellow-100 px-2 py-1 text-xs font-medium text-yellow-800 dark:bg-yellow-900 dark:text-yellow-200">
                            Affiliate
                          </span>
                        ) : (
                          <span class="rounded-full bg-gray-100 px-2 py-1 text-xs font-medium text-gray-800 dark:bg-gray-900 dark:text-gray-200">
                            Normal
                          </span>
                        )}
                      </td>
                      <td class="whitespace-nowrap px-6 py-4 text-sm text-gray-500 dark:text-gray-400">
                        {link.created}
                      </td>
                    </tr>
                  );
                })
              }
            </tbody>
          </table>
        </div>
      </div>

      <!-- Usage Example -->
      <div class="glass mt-8 rounded-xl p-6">
        <h3 class="mb-4 text-xl font-bold">Verwendung</h3>
        <div class="space-y-4">
          <div>
            <h4 class="mb-2 font-semibold">In Markdown/MDX:</h4>
            <code
              class="block rounded bg-gray-100 p-3 text-sm dark:bg-gray-800"
            >
              import RedirectLink from "@components/common/RedirectLink.astro";<br
              />
              &lt;RedirectLink id="amazon-example-1" text="Arduino Kit kaufen" /&gt;
            </code>
          </div>
          <div>
            <h4 class="mb-2 font-semibold">Direkter Link:</h4>
            <code
              class="block rounded bg-gray-100 p-3 text-sm dark:bg-gray-800"
            >
              [Arduino Kit](/redirect/?id=amazon-example-1)
            </code>
          </div>
        </div>
      </div>
    </div>
  </main>
</BaseLayout>
