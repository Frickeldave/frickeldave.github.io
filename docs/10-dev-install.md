# Installation

Die Installation der Entwicklungsumgebung erfolgt grundsätzlich über einen Container.

Die passende Docker-Konfiguration mit Dockerfile und `docker-compose.yaml` ist in
[10-dev-install-docker.md](./10-dev-install-docker.md) dokumentiert.
